# Copy this file to .env and fill in the values.
# .env is gitignored — never commit real secrets.

# Azure OpenAI (shared by both bots)
AZURE_OPENAI_API_KEY=
AZURE_OPENAI_API_BASE=https://filip-mfmovghf-swedencentral.cognitiveservices.azure.com
AZURE_OPENAI_DEPLOYMENT=gpt-5.4

# Telegram bot tokens — create two separate bots in @BotFather
TELEGRAM_ADMIN_TOKEN=       # token for the admin bot
TELEGRAM_VOLUNTEER_TOKEN=   # token for the volunteer bot

# frank-ingest MCP access
INGEST_API_KEY=             # same as the INGEST_API_KEY secret in Azure Container Apps
